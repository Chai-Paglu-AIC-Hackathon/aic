{
  "name": "aic-24.04-pixi",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",

  "remoteUser": "vscode",
  "updateRemoteUserUID": true,

  "containerEnv": {
    "DEBIAN_FRONTEND": "noninteractive",

    "PIXI_HOME": "/workspaces/aic/.pixi-home",
    "PIXI_CACHE_DIR": "/workspaces/aic/.pixi-cache",
    "PIXI_GLOBAL_CACHE_DIR": "/workspaces/aic/.pixi-cache",
    "PIXI_WORKSPACE_DIR": "/workspaces/aic/.pixi-workspace",

    "PATH": "/workspaces/aic/.pixi-home/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  },

  "runArgs": [
    "--init",
    "--ipc=host"
  ],

  "postCreateCommand": "bash -lc 'sudo apt-get update && sudo apt-get install -y --no-install-recommends curl ca-certificates git build-essential cmake pkg-config python3 python3-venv && curl -fsSL https://pixi.sh/install.sh | bash && pixi --version'"
}